{% set event = hookable_metadata.context.event %}
<div class="d-inline d-sm-block text-secondary pe-sm-3 pt-3">
    <span class="text-nowrap">{{ event.createdAt|date('Y-m-d') }}</span>
    <span class="text-nowrap">{{ event.createdAt|date('H:i:s') }}</span>
</div>
